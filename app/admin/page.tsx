import { isAdmin } from "@/lib/auth"; import { getPosts } from "@/lib/posts"; import { login,logout } from "./actions"; import { ArticleEditor } from "@/components/article-editor";
export const dynamic="force-dynamic";
export default async function Admin({searchParams}:{searchParams:Promise<{error?:string;saved?:string}>}){const qs=await searchParams;if(!(await isAdmin()))return <main className="admin-shell"><section className="login-card"><p className="eyebrow">Author access</p><h1>Welcome back.</h1><p>Enter your publishing password to continue.</p>{qs.error&&<p className="notice error">That password is not correct.</p>}<form action={login}><label>Password<input type="password" name="password" required autoFocus/></label><button>Open dashboard</button></form></section></main>;const posts=await getPosts(true);return <main className="admin-shell"><div className="admin-head"><div><p className="eyebrow">Author dashboard</p><h1>Write something worth keeping.</h1></div><form action={logout}><button className="quiet">Sign out</button></form></div>{qs.saved&&<p className="notice">Post saved successfully.</p>}<div className="admin-grid"><ArticleEditor/><aside className="drafts"><h2>Posts</h2>{posts.map(p=><div className="draft-row" key={p.slug}><div><strong>{p.title}</strong><small>{p.topic}</small></div><span className={p.status}>{p.status}</span></div>)}</aside></div></main>}
